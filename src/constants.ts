import { UnilogOptions } from "./interfaces/UnilogOptions"

export const NAMESPACE_PROVIDER = "Unilog::Namespace"
export const OPTIONS_PROVIDER = "Unilog::Options"

export const TL_LOGGER = "Logger"
export const TL_ACCUMULATOR = "Accumulator"

export const OPTIONS: UnilogOptions = {
  pino: {},
}
